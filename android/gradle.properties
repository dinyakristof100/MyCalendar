org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# ponytail: a pub cache a C:-n van, a projekt a D:-n, és a Kotlin inkrementális
# fordító nem tud relatív utat számolni két meghajtó között (IllegalArgumentException:
# "different roots"). Az app saját Kotlin kódja elenyésző, így ez alig kerül időbe.
# Ha egyszer a PUB_CACHE is a D:-re kerül, ez a sor törölhető.
kotlin.incremental=false
